DELIMITER &&
CREATE PROCEDURE DISPLAYP()
BEGIN
	SELECT * FROM student;
END &&
DELIMITER;

DELIMITER &&
CREATE PROCEDURE INSERTP(IN roll_no INT, IN name VARCHAR(10))
BEGIN
	INSERT INTO student(roll_no ,name) VALUES (roll_no , name);
END &&
DELIMITER;

DELIMITER &&
CREATE PROCEDURE UPDATEP(IN roll_no INT, IN name VARCHAR(10))
BEGIN
	UPDATE student SET name= name where roll_no = roll_no;
END &&
DELIMITER;

DELIMITER &&
CREATE PROCEDURE DELETEP(IN roll_no INT)
BEGIN
	delete from student where roll_no = roll_no;
END &&
DELIMITER;

DELIMITER &&
CREATE PROCEDURE OUTPP(OUT COUNT_STUD INT)
BEGIN
	SELECT COUNT(roll_no) INTO COUNT_STUD FROM student;
END &&
DELIMITER;

DELIMITER &&
CREATE PROCEDURE INOUTPP(INOUT COUNTSTUD INT, IN roll_no INT)
BEGIN
	SELECT COUNT(roll_no) into COUNTSTUD FROM student WHERE roll_no > roll_no;
END &&
DELIMITER;


DELIMITER &&
CREATE FUNCTION ADDITION(A INT,B INT)
RETURNS INTEGER
DETERMINISTIC
BEGIN
	DECLARE C INT;
	SET C = A+B;
RETURN C;
END &&
DELIMITER;

DELIMITER &&
CREATE FUNCTION MULTIPLICATION(A INT , B INT)
RETURNS INTEGER
DETERMINISTIC
BEGIN
	DECLARE C INT;
	SET C = A*B;
RETURN C;
END &&
DELIMITER;

DELIMITER &&
CREATE FUNCTION FACTORIALP(X INT)
RETURNS INTEGER
DETERMINISTIC
BEGIN
	DECLARE RESULT INT;
	DECLARE I INT;
	SET RESULT = 1;
	SET I = 1;
	WHILE I<=X DO
	SET RESULT = RESULT*I;
	SET I = I+1;
	END WHILE;
RETURN RESULT;
END &&
DELIMITER;

	


















